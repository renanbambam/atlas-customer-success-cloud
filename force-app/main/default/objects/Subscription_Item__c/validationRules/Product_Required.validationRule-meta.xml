<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Product_Required</fullName>
    <active>true</active>
    <description>Product2 does not allow restricted-delete lookups, so the field cannot be schema-required; this rule provides the same guarantee at the data layer.</description>
    <errorConditionFormula>ISBLANK(Product__c)</errorConditionFormula>
    <errorDisplayField>Product__c</errorDisplayField>
    <errorMessage>Every subscription item must reference a product.</errorMessage>
</ValidationRule>
